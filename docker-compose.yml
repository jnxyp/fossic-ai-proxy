services:
  proxy:
    build: .
    ports:
      - "18080:18080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./prompts:/app/prompts:ro
      - ./glossary:/app/glossary:ro
      - ./data:/app/data
    restart: unless-stopped
